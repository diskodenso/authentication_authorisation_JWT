# authentication_authorisation_JWT
Goal is to post new Users into the database with Signup formular and login User with Login formular

# JWT based authentication
Simple express app with token-based authentication.

Database: MongoDB
Input validation: Mongoose, Joi
Password hashing: bcryptjs
Token generation and validation: JWT